# basic-login-system
A simple login system works conditionally with if, elif, else statements. Each incorrect login attempt reduces the number of attempts, and the code closes when all attempts are incorrect.
